# 2026-09-22 发布记录

## 范围

发布此前本地完成的关键词内容优化、34 篇文章、教师打印资源、Finder 输出修复、搜索与导航改进及相关回归检查。问题与修复细节见 `../评审/2026-09-22-评审问题修复记录.md`。

生产仓库：`BogerHou/quickgetwell`；生产分支：`main`；生产站点：`https://quickgetwell.com`。通过现有 GitHub 推送触发 Cloudflare Pages 构建，构建产物为 `dist`。

## 发布授权与审核状态

在上一轮回复明确告知“目前尚未部署；人工编辑审核仍待完成”后，项目负责人指示：“如果没有其他问题的话，就提交上线吧。”

本次据此授权发布技术检查通过的版本。这是发布决定，不是对 37 项内容的逐项真人审校签核。`review-register.json` 保持 `human_review_pending`，审核人、日期和批准指纹保持空值；网站继续如实披露 AI 参与和人工审核尚未记录。

`npm run check:editorial` 因上述 37 项待审而未通过，`build:release` 的严格检查逻辑没有改动。本次沿用现有 `npm run build` 的 Cloudflare 流程，不把技术检查或 AI 评审伪装成该审核检查通过。后续默认使用已记录的人工审核流程；任何发布例外都应另行记录。

## 发布前验证

- `npm run check` 通过：1,080 种 Finder 分类组合及定向回归、整站生成、34 篇文章与本地链接。
- `git diff --check` 通过。
- 本轮此前已完成桌面和手机尺寸的浏览器复测，以及两种纸张 PDF 的逐页渲染检查；详见修复记录。
- 推送前核对 `main` 与 `origin/main` 无分叉。
- 公开提交仅包含网站源文件、公开素材、必要工具和文档；GSC 原始表格、运营分析、内部关键词研究、临时输出和系统元数据不纳入提交。

## 发布结果核验

推送后核对该提交的 Cloudflare Pages 检查结果，再运行 `npm run validate:live`。另核验线上文章、打印文件及其 `noindex` 响应头、站点地图和核心工具行为；以最终发布回执中的实际结果为准。
