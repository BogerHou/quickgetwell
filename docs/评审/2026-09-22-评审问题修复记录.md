# 独立评审问题修复记录

日期：2026-09-22。对应 [三个独立 Agent 网站评审](2026-09-22-三个独立Agent网站评审.md)。本轮修复现有功能与文案，没有新增页面或扩大功能范围。修改已生成到本地 `dist`，未部署。

## 问题与处理

| 评审发现 | 已实施的修复 | 主要位置 |
| --- | --- | --- |
| Finder 默认宣称团队已接手工作 | 移除未确认的工作覆盖、期限与项目安排；默认输出祝愿和尊重隐私的表达 | `script.js` |
| 输入具体帮助后仍附加通用承诺 | 自定义帮助替换通用帮助；三个结果分别为主消息加具体帮助、低压力消息加具体帮助、单独的具体帮助 | `script.js` |
| 六条结果同义重复、语句机械拼接 | 收敛为三条可独立使用的选项，删除重复关心和祝愿；使用直接自然的默认表达；清除不再使用的旧模板和评分代码 | `script.js` |
| 集体卡混用个人和集体人称 | 生成的正文与帮助片段统一使用 `we/our/us`；用户自己写的完整帮助句保留原文与原承诺者，结果说明会提示这一点 | `script.js` |
| 快捷控制看起来可以叠加，实际相互覆盖 | 改为 Short / Warm / Low-pressure，注明选择一种语气；选中状态跟随实际生效语气，下拉框与按钮同步 | `generate-pages.js`、`script.js`、`styles.css` |
| `printable`、`PDF` 和 `chemotherapy` 搜不到已有内容 | 将打印资源标题、标签和下载格式纳入搜索词；癌症专题增加 chemotherapy 别名 | `generate-pages.js` |
| 固定白字导航叠在浅色正文上 | 顶栏使用稳定深色背景和轻阴影；桌面与手机尺寸均复核 | `styles.css` |
| 从癌症专题打开 Finder 丢失病情 | 专题链接传递对应分类；癌症页带入 Serious illness + Supportive，其他匹配页传递关系、情境、语气或格式；无对应分类的页面标为 New message | `generate-pages.js`、`script.js` |
| 母亲、严重病情、癌症、同事文章的隐含前提或指导冲突 | 母亲页首选不再默认照顾全家，移除 for once；严重病情页优先中性关怀并标注亲近表达条件；癌症页缩小帮助承诺；同事幽默移除催返岗框架 | `content/pages.js` |

结果精简后额外检查了 Format 的实际作用：花束/礼物便签保留简短赠礼措辞，邮件与卡片保留称呼及帮助段落换行，短信保持紧凑。没有添加签名、发送邮件等新功能。输出是供用户选择的一条完整留言，不应把三条全部合并发送。

专题上下文只在首页读取允许列表中的四类参数；不会从 URL 导入姓名或帮助原文，也不会存储这些输入或把自动恢复分类记为一次使用事件。

## 验证结果

- `npm run check` 通过：语法、1,080 种 Finder 分类组合和定向回归、整站构建、34 篇文章及站内链接检查。
- Finder 回归覆盖：未确认工作承诺、重复句与重复关心、具体帮助替换、集体人称、用户完整原句、敏感情境语气调整、按钮状态、URL 参数允许列表、复制和统计事件隐私。
- 生成站点检查新增：打印资源与化疗关键词可发现，专题 Finder 参数合法，癌症页和上司页保留对应情境/关系。
- `git diff --check` 通过。

本地浏览器实际复测：

1. `printable` 和 `PDF` 能找到 Teacher，`chemotherapy` 能找到 Cancer，并能打开文章。
2. 癌症页的 Finder 链接打开工具后，Situation 为 Serious illness、Tone 为 Supportive。
3. 桌面癌症文章滚到 Choose wording 后，固定导航背景稳定、文字可读。
4. 手机尺寸 390 × 844 的首页工具无横向溢出；Short 后点击 Warm，只有 Warm 保持选中，Tone 为 Heartfelt。测试后恢复原视口。
5. Boss / Serious illness / Funny 会说明并显示 Professional，三条输出均没有工作已安排好的承诺。
6. Partner / Chronic illness / Heartfelt / Email，填写虚构姓名 Alex 和 `I can bring dinner on Thursday.` 后，没有额外跑腿、提醒或陪伴承诺；复制第一条到浏览器会话剪贴板，称呼与帮助段落的空行保留。
7. Friend / After surgery / Supportive / Group card，帮助片段 `drop off dinner this week` 使用 We；改填完整个人原句时保留 I，并显示解释。
8. 花束便签输出简短；用键盘清空姓名与帮助后，结果立即移除这两项输入。没有观察到页面脚本错误。
9. 实际阅读修订后的 Mom、Serious illness、Coworker、Cancer 页面，确认首选、适用条件和幽默措辞已更新。

## 打印卡片检查补全

上轮内置 PDF 查看器灰页没有被认定为站点故障。本轮直接读取并渲染源文件，补足文件层面的检查：

- `assets/printables/teacher-get-well-cards-a4-v1.pdf`：两页，595.2756 × 841.8898 pt，符合 A4 尺寸。
- `assets/printables/teacher-get-well-cards-letter-v1.pdf`：两页，612 × 792 pt，符合 US Letter 尺寸。
- 两份文件共四页逐页查看了渲染图；第一页涂色卡、第二页 24 个班级署名格均显示完整，没有观察到裁切、重叠、乱码或黑块。
- 没有修改 PDF 或卡片设计；没有做实体打印测试。

## 边界

这是技术检查和 AI 内容复核，不是英语母语者访谈、真人编辑签核或真实用户转化实验。人工审核登记仍保持待审核，未填入虚构审核人。没有据此推断流量增长、证明新增功能的需求规模，或宣称全站所有英文已经完成逐句人工审校。

只验证了本地生成版本；没有部署、推送、核查线上统计接收，或向外部邮件/聊天工具发送内容。剪贴板验证限浏览器会话，未验证系统剪贴板和外部编辑器粘贴。
